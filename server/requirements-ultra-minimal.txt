# Ultra minimal - guaranteed to work on Render
fastapi==0.109.0
uvicorn==0.27.0
python-dotenv==1.0.0
pyyaml==6.0.1

# Only add these if API breaks without them:
# numpy==1.26.4
# pandas==2.2.0
# joblib==1.4.0
